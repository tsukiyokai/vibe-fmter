---
name: vibe-fmt
description: >
  中英文混排Markdown/TXT文档格式化工具。自动修正排版规则（中英文间距、标点全半角、
  数字格式、Markdown结构）并检查文档结构（标题层级、段落长度、列表一致性）。
  当用户提到'排版'、'格式化'、'美化文档'、'vibe-fmt'、'format'、'fmt'，
  或要求整理markdown/txt文档格式时触发。输入文件路径，原地修改文件。
---

# vibe-fmt

中英文混排文档格式化，两阶段管线：脚本处理确定性规则，Claude处理结构判断。

## 工作流

1. 读取$ARGUMENTS指定的文件路径（支持绝对路径或相对路径）
2. 检测是否需要段落合并（见"PDF断行检测"）
3. 执行 `scripts/format.py` 处理排版层规则，必要时加 `-j` 标志
4. 读取脚本输出的JSON摘要，了解已修改项
5. 读取脚本处理后的文件，执行结构层检查(S1-S3)
6. 如有结构问题，直接修正文件
7. 输出最终修改摘要

## 阶段1: 脚本（排版层）

运行脚本：

```bash
python3 <skill_dir>/scripts/format.py [-j|--join-lines] <file_path>
```

其中 `<skill_dir>` 是本SKILL.md所在目录的绝对路径。

`-j` 启用R0段落合并（见下方"PDF断行检测"）。

脚本自动处理：
- R0: 合并PDF提取的断行（需 `-j` 标志，见"PDF断行检测"）
- R1a: 中英文/中数之间删除空格（紧凑模式）
- R1c: 全角标点与其他字符之间删除多余空格
- R2: 全半角标点按语境修正
- R2b: 括号配对修复（开闭括号全半角不一致时，按内容语种统一）
- R3: 全角数字转半角
- R4: 重复标点去重（保留……）
- R5: 标题前后空行
- R6: 列表缩进规范化（2空格/级）
- R7: 表格列对齐
- R8: 代码块内行尾 `#`/`//` 注释对齐（根据语言自动选择标记）
- R9: 裸URL后紧跟全角字符时自动用 `<>` 包裹
- R10: 列表标记统一为 `-`（替换 `*` 和 `+`）

脚本输出JSON到stdout，格式：`{"total_changes": N, "by_rule": {...}, "file": "..."}`

### PDF断行检测

PDF提取的文档（pdftotext/pymupdf等）会按页面宽度在固定列截断行。检测方法：读取文件前100行非特殊行（跳过标题、列表、空行、分隔符），统计以CJK表意字符结尾的行占比。

- 占比 > 15%: 几乎确定是PDF提取，加 `-j`
- 占比 < 5%: 正常markdown，不加 `-j`
- 5%-15%: 读几行确认是否断句不自然，酌情决定

典型症状：一个语义连贯的句子被拆成两行，第一行以汉字（非标点）结尾，第二行以汉字开头直接续写。

## 阶段2: 结构层检查

脚本完成后，读取修改后的文件，逐项检查：

- S1 标题层级不跳级：h1下不应直接出现h3，依次递进
- S2 同级标题不孤立：某级标题至少出现两次，否则该层级无意义
- S3 段落不超过7行：超长段落拆分为多段或转为列表（博客/散文类文档可跳过此项）

注：S4（列表标记统一）已移入脚本层R10自动处理。

发现问题时直接修正文件。如果某项修正需要理解语义（如段落在哪里拆分），选择最合理的拆分点。

## 输出格式

完成后输出修改摘要：

```
vibe-fmt完成:
- 排版层: N处修改 (R1间距: x, R2标点: x, ...)
- 结构层: M处修改 (S1标题层级: x, S3段落拆分: x, ...)
- 文件: <path>
```

## 规则详情

完整规则及示例见[references/rules.md](references/rules.md)。
